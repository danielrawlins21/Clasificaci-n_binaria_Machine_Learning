\section{Modelo orientado a Recall}

El objetivo de este bloque es maximizar la captación de clientes con churn (sensibilidad) con un protocolo estricto de validación. El flujo inicial se construyó con un árbol de decisión y una MLP optimizada en \textit{Recall}; después se afinó con modelos de ensamble.

Todas las decisiones de selección (hiperparámetros y umbral) se tomaron exclusivamente en validación interna estratificada, reservando el conjunto de test para evaluación final.

\subsection{Selección inicial de variables con árbol}

En la etapa base se entrenó un árbol de decisión y se seleccionaron las variables más relevantes por importancia (top-$k$, con $k=8$). Este paso permitió construir un primer subconjunto dirigido a Recall, sobre el cual se entrenó el modelo neuronal de referencia del bloque.

El baseline de esta etapa (\texttt{Tree\_MLP\_Recall}) alcanzó un \textbf{Recall de 0.6099} en test, estableciendo el punto de partida para la fase de mejora.

\subsection{Optimización final del modelo de Recall}

Para incrementar sensibilidad se ejecutó una búsqueda aleatoria sobre modelos de ensamble de árboles (Random Forest y Extra Trees), con selección primaria por \textit{CV Recall} y guardrails para evitar soluciones degeneradas: restricción de precisión mínima y control de PR-AUC durante la selección de umbral.

Con el fin de hacer trazable la búsqueda paramétrica, la \cref{tab:rec_hparams_mlp} resume el baseline neuronal (árbol + MLP) y la \cref{tab:rec_hparams_et} documenta el ajuste del modelo final de ensamble.

\begin{lstlisting}[caption={Búsqueda compacta en Recall},label={lst:recall_search}]
grid_rec = RandomizedSearchCV(et_estimator, param_distributions, n_iter=24,
							  scoring="recall", cv=cv, random_state=cfg.random_state)
threshold_rec, _ = select_threshold(y_train.to_numpy(), proba_cv_rec,
									policy_rec, np.array(default_cfg["threshold_grid"]))
\end{lstlisting}

\begin{table}[h]
\centering
\small
\caption{Recall (baseline MLP): hiperparámetros probados vs elegidos}
\label{tab:rec_hparams_mlp}
\begin{tabular}{p{0.36\textwidth}p{0.38\textwidth}p{0.20\textwidth}}
\toprule
\textbf{Hiperparámetro} & \textbf{Valores probados} & \textbf{Elegido} \\
\midrule
\texttt{hidden\_layer\_sizes} & \texttt{(32,)}, \texttt{(64,)}, \texttt{(32,16)} & \texttt{(64,)} \\
\texttt{activation} & \texttt{relu}, \texttt{tanh} & \texttt{relu} \\
\texttt{alpha} & \texttt{1e-4}, \texttt{1e-3}, \texttt{1e-2} & \texttt{1e-2} \\
\texttt{learning\_rate\_init} & \texttt{1e-3}, \texttt{5e-3} & \texttt{5e-3} \\
\texttt{solver} & \texttt{adam} & \texttt{adam} \\
\texttt{max\_iter} & \texttt{1000} & \texttt{1000} \\
\texttt{early\_stopping} & \texttt{True} & \texttt{True} \\
\bottomrule
\end{tabular}
\end{table}

La rejilla MLP evaluó 36 combinaciones con validación cruzada estratificada y scoring primario en Recall.

El mejor candidato fue \textbf{Extra Trees}, adoptado como modelo final de Recall bajo la etiqueta \texttt{ET\_Recall}. El umbral final seleccionado en validación fue \textbf{0.14}.

\begin{table}[h]
\centering
\small
\caption{Recall (modelo final ET): hiperparámetros probados vs elegidos}
\label{tab:rec_hparams_et}
\begin{tabular}{p{0.36\textwidth}p{0.38\textwidth}p{0.20\textwidth}}
\toprule
\textbf{Hiperparámetro} & \textbf{Valores probados} & \textbf{Elegido} \\
\midrule
\texttt{n\_estimators} & \texttt{300}, \texttt{500}, \texttt{700} & \texttt{300} \\
\texttt{max\_depth} & \texttt{None}, \texttt{10}, \texttt{14}, \texttt{20} & \texttt{14} \\
\texttt{min\_samples\_leaf} & \texttt{1}, \texttt{2}, \texttt{4}, \texttt{8} & \texttt{8} \\
\texttt{max\_features} & \texttt{sqrt}, \texttt{0.6}, \texttt{0.8} & \texttt{0.6} \\
\texttt{class\_weight} & \texttt{None}, \texttt{balanced} & \texttt{balanced} \\
\bottomrule
\end{tabular}
\end{table}

Para ET se aplicó \texttt{RandomizedSearchCV} con 24 iteraciones y ajuste de threshold con guardrails de precisión mínima y PR-AUC.

\begin{table}[h]
\centering
\caption{Resultados finales del modelo orientado a Recall (test)}
\begin{tabular}{|l|c|}
\hline
Modelo final & \texttt{ET\_Recall} \\
\hline
Umbral de decisión & 0.14 \\
\hline
Recall & 0.9433 \\
\hline
Precision & 0.2989 \\
\hline
Accuracy & 0.5480 \\
\hline
F1-score & 0.4539 \\
\hline
ROC-AUC & 0.9265 \\
\hline
PR-AUC & 0.8809 \\
\hline
Matriz de confusión $(TN,FP,FN,TP)$ & $(255,312,8,133)$ \\
\hline
\end{tabular}
\end{table}

La mejora sobre el baseline fue notable: $0.9433 - 0.6099 = 0.3334$ puntos absolutos de Recall. Este resultado confirma que el modelo final prioriza fuertemente la detección de positivos (coste de falsos negativos muy bajo), a cambio de una reducción en precisión y accuracy, comportamiento coherente con un objetivo de negocio centrado en cobertura máxima de churn.
